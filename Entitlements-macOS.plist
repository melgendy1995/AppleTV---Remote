<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- App Sandbox is disabled so the app can spawn its bundled Python
         interpreter as a subprocess. To re-enable, we'd need to ship the
         Python binary signed with the same Team ID and add
         com.apple.security.inherit + com.apple.security.cs.allow-jit etc. -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <!-- Allow loading the bundled Python framework which isn't signed by
         Apple. Needed when codesign is ad-hoc. -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
